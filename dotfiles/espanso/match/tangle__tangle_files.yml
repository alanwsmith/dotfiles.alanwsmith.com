matches:
  - trigger: ":tangle;"
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - bash
            - "%CONFIG%/scripts/tangle.espanso"
          trim: false
