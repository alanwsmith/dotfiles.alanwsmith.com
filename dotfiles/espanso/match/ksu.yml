matches:
  - trigger: ksu;
    replace: '{{output}}'
    vars:
      - name: output
        type: script
        params:
          args:
            - bash
            - '%CONFIG%/scripts/ksu.espanso'
          trim: true
