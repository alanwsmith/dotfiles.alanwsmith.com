name: tlove__twitter_love
parent: default

matches:
  - trigger: "tlove;"
    replace: "{{output}}"
    vars:
      - name: output
        type: script 
        params:
          args:
            - bash
            - "%CONFIG%/scripts/tlove.espanso"
          trim: false
